// File: native_modules.gradle

import org.apache.tools.ant.filters.FixCRLF

// This script allows Gradle to detect and handle react-native linked modules and dependencies.
ext {
    reactNativeDir = rootProject.projectDir.getAbsoluteFile().getParentFile().getAbsolutePath()
}

apply from: "$reactNativeDir/node_modules/react-native/react.gradle"

// Handle native modules
apply from: file("$rootDir/../node_modules/@react-native-community/cli-platform-android/native_modules.gradle")
