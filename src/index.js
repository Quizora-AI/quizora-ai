
// This file serves as an entry point for the web version of the app
import { registerRootComponent } from 'expo';
import App from '../App'; // Import the root App component

registerRootComponent(App);
